/** The product name. Always lowercase, including at the start of a sentence. */
export const PRODUCT_NAME = "lurk";

/** The public origin of the hosted instance. */
export const PRODUCT_URL = "https://lurk.so";

/** Where every outbound AnyAPI link lands, tagged so AnyAPI can see lurk sent the visit. */
export const ANYAPI_URL = "https://getanyapi.com/?utm_source=lurk";

/** The name as it reads next to its data provider: headers, titles, agent-facing text. */
export const PRODUCT_NAME_WITH_PROVIDER = `${PRODUCT_NAME} by AnyAPI`;
